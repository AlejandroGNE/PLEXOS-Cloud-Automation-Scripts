---
name: replace-with-project-slug
description: Use for work on the named PLEXOS project and model snapshot described below.
---

# Project-specific PLEXOS use case

Replace this file's placeholders, then save it as `UseCases/<project-slug>/SKILL.md`. Keep general SDK mechanics in the repository documentation; this file records **model-specific, verified meaning**.

## Scope and source

- Project and model name:
- Source version or changeset ID:
- Date inspected:
- PLEXOS, Cloud CLI, and `plexos_sdk` versions used:
- Input files required but intentionally excluded from this public repository:

## Verified model map

| Claim | Input evidence: class, object, collection, property, tag, date | Output evidence, if applicable | Validation |
|---|---|---|---|
| Example: which Model enables a policy | Replace with actual SDK inspection | Replace with solution series, if used | Replace with export/reimport or run result |

Describe assumptions, exclusions, and units. Distinguish what the XML explicitly states from what an analyst inferred from results or naming conventions.

## Reproduction

List the scripts/commands that reproduce each claim using a **separate working copy** of the model. Record the output files to inspect and the checks that determine success. Do not include private paths, credentials, or model data.

## Open questions and change triggers

- What has not been validated?
- Which model changes would require this map to be refreshed?
